<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.paulsentribe.dialview.MainActivity">

    <com.paulsentribe.dialview.SpeedometerView
        android:layout_width="400dp"
        android:layout_height="wrap_content"
        app:stitle="RPM"
        app:labelTextSize="20dp"
        app:valueTextSize="20dp"
        app:titleTextSize="10dp"
        app:maxSpeed="2700"
        app:minSpeed="0"
        app:majorTickStep="500.0"
        app:minorTicks="4"
        android:id="@+id/speedoMeterView"/>

    <SeekBar
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        android:layout_below="@id/speedoMeterView"
        android:id="@+id/seekBar"
        android:max="2700" />

</RelativeLayout>
